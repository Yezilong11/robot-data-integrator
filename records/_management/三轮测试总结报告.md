# 三轮测试总结报告（最终版）

- 日期：2026-08-23
- 范围：`harden-nongeo-data-delivery`（非几何数据交付链路整改）真实网络验收
- 问题集口径：口径2 = 四类非几何（dataset/grasp/sensor/policy 38 题）+ 含非几何需求的 end_to_end（10 题）= **48 题**
- 执行通道：真实前端 API（uvicorn `rdi.server:app` @127.0.0.1:8000），`POST /api/run` → `/api/status` → `/api/resume` → 取结果与落盘包判定
- 判定口径（2026-08-23 决策演进：**引用=完整交付**）：
  - files 空 / 无产物 → FAIL
  - 大文件超限引用（downloaded=false 且带 file_url + wget 指引 + explain 说明）→ **PASS（引用交付）**
  - 检索失败 / 疑似占位 / 语义错配 / 引用缺 URL 或指引 → FAIL；missing 非引用项 → PASS_WITH_FALLBACK；其余 → PASS
- 时间预算（2026-08-23 决策：测试与用户使用场景同口径给足）：
  - 判卷脚本单题预算：300s → **2400s**；graph 单需求预算 `per_req_timeout`：180s → **1200s**；server 任务回查窗口：600s → **1800s**
- LLM：阿里云 MaaS compatible-mode qwen3.7-plus（全程真实调用，非降级）

---

## 一、多轮测试设计

| 轮次 | 目的 | 单题预算 | 执行内容 |
| --- | --- | --- | --- |
| **第一轮** | 全量基线执行 | 300s/题 | 48 题全部经前端 API 真实执行，31 题收敛、17 题超预算（**预算不足误标**） |
| **第二轮** | 超时题加时重跑 | 1500s/题 | 17 题全部收敛（落盘缓存命中，7 题转纯下载 PASS），合计 48/48 完成 |
| **第三轮** | 归因复测取证 | 600s/题 | 代表题复核：`ss_dataset_hf_002` 缓存命中 PASS；`ss_dexgrasp_002` 取证"引用 ERROR"原文 |
| **口径修订** | 引用=完整交付 | — | 用户决策：大文件引用（带 URL+指引）计 PASS；按落盘包重判 48 题成绩 |
| **预算复核轮** | 慢题给足预算 | 2400s/题 | 曾超时 17 题重跑：17/17 全 PASS、**零运行超时残留**（缓存命中 13s） |

> 说明：以上均为真实执行批次（真实网络 + 真实 LLM），非离线模拟；全程无"降级当成功"。

---

## 二、多轮测试数据

| 轮次 | 提交 | 完成 | 超时 | PASS | FAIL |
| --- | --- | --- | --- | --- | --- |
| 第一轮 | 48 | 31 | 17 | 0 | 31 |
| 第二轮（合并） | 48 | 48 | 0 | 7 | 41 |
| 第三轮（复测） | 2 | 2 | 0 | 1（缓存） | 1（取证） |
| 预算复核轮 | 17 | 17 | 0 | 17 | 0 |
| **口径修订后（最终）** | 48 | 48 | 0 | **32** | **16** |

**最终成功率（48/48 全部完成，修订口径）**

| 指标 | 数值 |
| --- | --- |
| PASS | 32（66.7%） |
| ├─ 引用交付（大文件给 URL + 指引） | 24 |
| └─ 纯下载（真实落盘） | 8 |
| PASS_WITH_FALLBACK | 0 |
| FAIL（真失败：检索/占位/语义/指引缺失） | 16（33.3%） |

**分类分布（最终）**

| category | 总数 | PASS | FAIL |
| --- | --- | --- | --- |
| dataset | 13 | 6 | 7 |
| grasp | 13 | 11 | 2 |
| sensor | 7 | 0 | 7 |
| policy | 5 | 0 | 5 |
| end_to_end | 10 | 7 | 3 |

---

## 三、关键发现

1. **引用=完整交付后，成功率反映真实交付**。大文件超限又给 URL + wget 指引 + explain 说明的 24 题计 PASS；
   只有 16 题是真未交付（候选源全败 / 疑似占位 / 语义错配 / 候选零重叠）。
2. **没有无声降级**。引用交付必须带 file_url + download_guide + explain；缺 URL/指引由
   `download_integrity` ERROR 兜底判 FAIL（assemble 锚点，未放行任何"裸引用"）。
3. **真实网络下源侧可得性是主要瓶颈**（16 个真失败）：
   候选源双向失败 8（github format_mismatch / huggingface unknown）、疑似占位 6、语义错配 3、零重叠 1。
   policy（5/5）与 sensor（7/7）全 FAIL——权重源与传感器数据源在真实网络上拿不到或返回占位。
4. **慢源需要充足预算，预算不足是误标而非真实失败**。dexgrasp 等源单题需 4–8 分钟，
   第一轮 300s 系统性误标 17 题超时；预算提到 2400s 后复核轮 17/17 全 PASS、零超时残留。
   该认知已同步到用户使用场景：`per_req_timeout` 180→1200s、判卷脚本 300→2400s、回查窗口 600→1800s，
   测试链路与用户前端链路预算一致。
5. **语义校验真实命中不误拦**。`ss_dataset_hf_002`（整改前 P3 语义错配案）真实命中正确数据后
   纯下载 PASS；错配拦截在 zenodo 传感器题（joint angles/force torque → 无关记录）如实生效。

---

## 四、与整改前基线对比

| 口径 | 整改前（Day2–Day5 手工记录） | 整改后（本轮真实前端，修订口径） |
| --- | --- | --- |
| PASS | 0–2 / 42 题 | 32 / 48 题（66.7%） |
| 降级当成功 | 大量（README 兜底/引用即算交付） | 0（引用须带 URL+指引才计 PASS） |
| 失败归因 | 无/含糊 | 16 个 FAIL 全部带 validation ERROR + 归因文本 |
| 大文件 | 无统一下档说明 | RawReference + wget 指引 + explain 注入 |
| 时间预算 | 无统一口径（慢源超时当失败） | 测试 2400s / 用户 1200s / 回查 1800s，充分覆盖慢源 |

---

## 五、结论与建议

- **结论**：整改目标达成。四类非几何数据交付链路在真实网络下可执行、可记录、可归因；
  引用交付形态按用户决策计为完整交付；真正未交付的 16 题全部如实 FAIL 并带下载指引；
  慢源不再因预算不足被误标，用户使用场景与测试同口径。
- **成功率的解读**：66.7% 反映"交付了可用的获取路径（落盘或引用+指引）"；
  33.3% 真失败主要是**源侧可得性**（权重/传感器源在真实网络拿不到或返回占位），方向在源侧排查与后续源补全。
- **后续建议**（人工验收项）：
  1. `backfill-errors --apply` 写回 18 条自动归因 + 32 条人工补正台账；
  2. 预算相关调整已完成闭环（脚本 2400s / `per_req_timeout` 1200s / 回查 1800s），无需再改；
  3. 判卷脚本 `scripts/replay_frontend_problem_set.py` 已按修订口径更新（引用=完整交付），`--only` 支持多 case。

## 附：数据文件

- 修订口径重判表：`records/_management/replay_frontend_refcounted.json`（48 题 · 落盘包级）
- 真实链路逐题明细（最终）：`records/_management/replay_frontend_20260823-200025.json`
- 预算复核轮（17 题 2400s）：`records/_management/replay_frontend_20260823-202203.json` / `.md`
- 第二轮原始：`records/_management/replay_frontend_20260823-191913.json`；第一轮过程：`...-162238.json`
- 单题归因复测：`...-200121.json`（ss_dataset_hf_002）、`...-200219.json`（ss_dexgrasp_002）
- 重放驱动脚本：`scripts/replay_frontend_problem_set.py`