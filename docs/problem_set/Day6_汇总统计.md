# Day 6：汇总统计与交付收尾（Day6-8 压缩合并）

> 2026-08-17 组长决定：原 Day6（汇总统计）+ Day7（复盘改进）+ Day8（交付验收）合并为一天，5 人协作当日完成。
> 删除了 `Day7_复盘与改进建议.md` 与 `Day8_交付验收.md`，本文件为唯一收尾计划。

## 当日目标

基于冻结的记录集一天内走完：全部统计（总体指标、分维度、失败原因 TOP3）→ 复盘归因（FAIL/PWF 根因）→ 终审验收与交付（`problem_set/` + `records/`）。统计数字必须与记录一致，验收线必须达标。

## 各角色详细工作

### F（质量工程师，统计 + 复盘归因汇总 + 交付整理）

1. 计算总体指标：总题数、通过率（PASS/总）、降级率（PASS_WITH_FALLBACK/总）、失败率（FAIL/总）。
2. 分维度统计：按语言（ZH/EN/MIX）、数据源、需求类型分组成功率。
3. 失败原因分布：分类码计数与占比，TOP 3。
4. 生成《统计表》（含原始 case 明细，可回溯）。
5. 归因记录类问题：判定不一致、字段缺失、降级未显式等记录质量问题。
6. 汇总全员归因结论，形成《改进建议清单》初稿（分 P0/P1/P2，指向 adapter/skill/node/prompt）。
7. 整理交付物：`problem_set/` + `records/` 目录核对、统计摘要生成、交付材料清单。

### C（数据工程师）

1. 向 F 提交数据源可用性汇总表（探活 + 执行观察合并，`data_source_availability.md` 定稿）。
2. 归因数据源类 PWF：P2_RETRIEVE / P3_SOURCE 每条给出根因（指向具体 adapter 或上游限制）。
3. 发言：数据源可用性表反映的趋势（哪些源稳定、哪些源需换路径）。
4. 配合 A 验收数据源相关条目；认领数据源类改进建议。

### D（机器人工程师）

1. 向 F 提交可加载性验证结论汇总（`可加载性验证结论.md` 定稿）。
2. 归因格式/仿真类 PWF：P4_FORMAT / P5_RUNTIME 每条给出根因（xacro/glb/python 场景等，依据 `P4_P5失败清单.md`）。
3. 发言：可加载性验证暴露的格式合规问题。
4. 配合 A 验收格式/仿真相关条目；认领格式/仿真类改进建议。

### E（产品工程师）

1. 向 F 提交环境信息汇总（LLM 模型分桶数据）。
2. 归因流程类 PWF：P6_FRONTEND 每条给出根因（interrupt/resume/UI）。
3. 交付操作手册最终版归档（含踩坑记录）；确认流程类 case 截图规范落地。
4. 配合 A 验收前端/流程相关条目；认领前端/流程类改进建议。

### A（组长/架构师，主持 + 终审）

1. 复核 F 的统计数字：抽样比对 5 条原始记录，确认统计口径无错。
2. 主持复盘：基于统计初稿逐条过 FAIL / PASS_WITH_FALLBACK case，对每条根因定性（系统缺陷 / 问题集设计不当 / 环境因素 / 数据源外部限制）。
3. 裁定改进建议优先级（P0 必改 / P1 应改 / P2 可改），确认《改进建议清单》定稿。
4. 终审 `problem_set/` + `records/`：记录完整率 ≥ 90%、截图规范、验收线 P0 ≥ 8/11 可用、统计口径正确。
5. 宣布验收结论，向团队确认交付；沉淀下一轮问题集建议（本次暴露的边界场景升级为新题目）。

## 与 A 的对接点

| 对接人 | 对接内容 | 期望输出 |
|---|---|---|
| F | 提交统计初稿 + 交付物清单 | A 复核数字与口径、终审 |
| C/D/E | 提交领域汇总材料 + 根因归因 | F 合并入统计与建议清单 |
| C/D/E/F | 复盘会决议 | A 定性并定 P0/P1/P2，F 记录存档 |
| F | 验收整改项 | A 拍板补齐或豁免 |

## 当日出口标准

- [ ] 统计初稿完成且数字与记录一致；数据源可用性表完成；失败原因 TOP 3 明确
- [ ] 复盘完成，所有 FAIL/PWF 有根因；《改进建议清单》定稿（分 P0/P1/P2，指向模块）
- [ ] 验收线全部达标，A 宣布验收通过
- [ ] `problem_set/` + `records/` 两个文件夹交付；统计摘要 + 改进建议清单归档
- [ ] 下一轮问题集建议沉淀

## 与 Day5 的衔接

- Day5 完成补测与记录冻结（F 冻结后 `records/` 不再改动，改动需 A 批准）。
- `ms_008` 若在 Day5 未补跑，作为已知遗留（P1、单需求路径未覆盖）如实记录，不阻断 P0 验收线。
