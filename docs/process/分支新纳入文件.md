# 分支新纳入文件

> 分支：`feat/problem-set`（Day1 问题集定稿与执行体系搭建）
> 提交：`59f2cde`（73 files changed，+7187 / -127）
> 时间：2026-08-12
> 说明：本分支新增了问题集定稿、执行记录体系、交付物归档三套目录，以及 8 天执行计划与口径文档；同时修复了代码库遗留的 ruff/mypy 类型检查问题，使 CI 与 pre-commit 全绿。

## 一、新增目录结构（3 套正式目录）

| 目录 | 用途 |
|---|---|
| `problem_set/` | 正式问题集载体：题目、维护说明、评审清单 |
| `records/` | 执行记录体系：记录模板、任务分配、探活数据、每日统计 |
| `deliverables/` | 组员交付物归档（按天 + 按人，`deliverables/day<N>/<角色>/`） |
| `scripts/manage_test_records.py` | F 的自动管理工具（初始化记录/完整性校验/静默降级检查/进度刷新/统计/Day8 严格验收），从根目录移入 `scripts/` 并修复路径 |

## 二、问题集定稿（problem_set/）

| 文件 | 说明 |
|---|---|
| `problem_set/problem_set.json` | 63 题正式问题集：55 单源 + 8 多源，P0 共 11 道，覆盖全部 15 个 DataSource |
| `problem_set/README.md` | 问题集维护说明（修改规则、校验命令、源计数口径） |
| `problem_set/review_checklist.md` | A 评审清单，2026-08-12 已签收（通过） |

## 三、执行记录体系（records/）

| 文件 | 说明 |
|---|---|
| `records/README.md` | 执行记录使用说明（F 每日操作、状态/截图/判定口径、记录字段约定） |
| `records/_templates/record.template.json` | record.json 模板 |
| `records/_templates/retrieve_item.example.json` | retrieve[] 单条记录字段示例 |
| `records/_templates/screenshots/README.md` | 截图命名规范（01_input/02_progress/03_package/04_validation + 05_error） |
| `records/_management/assignments.csv` | 63 题任务分配表（C=44 单源、D=11 单源、A/E/F 多源） |
| `records/_management/progress.csv` | 执行进度表（脚本生成） |
| `records/_management/quality_report.json` | 质量报告（脚本生成） |
| `records/_management/statistics_summary.md` | 统计摘要（脚本生成） |
| `records/_probe/probe_20260812.json` | Day1 数据源探活结果 |

## 四、交付物归档（deliverables/）

| 文件 | 来源 |
|---|---|
| `deliverables/README.md` | 归档规则与 day1 清单 |
| `deliverables/day1/C/problem_set_c_draft.json` | C 的 43 题草稿（已合并进 63 题定稿） |
| `deliverables/day1/C/probe_20260812(1).json` | C 的探活原始数据 |
| `deliverables/day1/C/probe_20260812_summary(1).json` | C 的探活汇总 |
| `deliverables/day1/C/角色C问题诊断与解决步骤报告.md` | C 的诊断报告 |
| `deliverables/day1/D/draft_D_robot_urdf_mesh_sim_config.json` | D 的 31 题草稿（已合并进 63 题定稿） |
| `deliverables/day1/E/rdi_stability_test_checklist.md` | E 的稳定性测试清单 |
| `deliverables/day1/E/前端操作手册.docx` | E 的前端操作手册（执行期参考） |

## 五、计划与口径文档（根目录）

| 文件 | 说明 |
|---|---|
| `Day1_问题集定稿与环境准备.md` ~ `Day6_汇总统计.md` | 6 天执行计划（每日目标、分工、对接点、出口标准；Day7/Day8 已压缩合并入 Day6） |
| `问题集构建策略.md` | v3.0 两层架构（单源主导 + 多源混合）、ACDEF 分工 |
| `问题集规模与源配额.md` | 各数据源题数与配额表 |
| `判定口径纪要_C数据源类.md` | PASS / PASS_WITH_FALLBACK / FAIL 判定口径（P0 验收线 ≥ 2/3 已确认） |
| `问题集执行_已知风险.md` | 已知风险跟踪（超大归档 grasp、DexGrasp raw 兜底、网络依赖等） |

## 六、配套代码改动（本次修复，非新增文件）

- 配置对齐：`.pre-commit-config.yaml`（mypy 钩子 `files: ^src/` + `entry: uv run mypy`）、`pyproject.toml`（mypy `exclude = "tests/"`、mujoco override）
- src 类型修复：`adapters/base.py`（资产并发下载类型收窄、Any 显式转换）、`models/common.py`（DataReqType.zh）、`graph/nodes/`（assemble/validate/retrieve_data）、`skills/`（code_parse/dataset_parse）、`intelligence/`（client/embedding）等
- 测试与数据源适配：`tests/unit/adapters/test_base.py`（新增 meshdir 测试）、`test_dexgrasp.py`、`test_huggingface.py`（base_url 随 settings）
- 适配器功能：`adapters/base.py`（MJCF meshdir/texturedir 目录基准 + 资产并发下载）、`adapters/dexgrasp.py`（空结果回退单关键词）

## 七、验证状态

- `uv run pre-commit run --all-files`：9 个钩子全绿
- `uv run pytest tests -m "not integration"`：727 passed / 9 skipped
- `uv run ruff check src tests`、`uv run ruff format --check src tests`、`uv run mypy src`：全过
