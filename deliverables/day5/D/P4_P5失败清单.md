# Day 5｜D：P4_FORMAT / P5_RUNTIME 失败清单

> 角色：D（机器人工程师）　整理日期：2026-08-17
> 依据：`docs/problem_set/Day5_执行截止与补测缓冲.md`（D 任务 2）+ `docs/problem_set/问题集构建策略.md` §6.3.4
> 数据来源：① day3/day4 交付物首轮与修复轮的 record/README 记录；② 2026-08-17 前端真实流程补测中出现的问题；③ 各 case 当前 `records/<case_id>/record.json`（当前记录中 P4/P5 相关条目均已消除，0 FAIL）
> 说明：条目均为**历史失败或残余风险**，每条含 case_id、现象、初步定位、状态；当前 13 个补测 case 的最终判定见 `README.md` §2.2（0 FAIL）。

## 1. P4_FORMAT（格式错配）

| case_id | req_id | 失败码 | 现象 | 初步定位 | 状态 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| ss_robotiq_001 | req_000 | P4_FORMAT | RobotiqAdapter 命中 `ros-industrial-attic/robotiq` 的 `.xacro` 包装文件（仅 include + 宏调用），assemble 按 raw_bytes 原样落盘为 `robots/req_000.urdf` → yourdfpy 加载 `LOAD_OK links=0 joints=0` 空壳（内容不可用却标 format=urdf/complete） | 装配层未走 xacro 展开分支，xacro→URDF 展开缺失 | ✅ 已修复（robotiq 适配器内置轻量 xacro 展开器：include 内联 + 宏展开 + `${expr}` 求值）→ 重测 PASS（11 links/10 joints） |
| ss_robotiq_002 | req_000 | P4_FORMAT | 同 001（xacro 空壳）；另 GitHub 源对 robot_urdf 返回 markdown README，检索期未预检格式，占用命中位置 | 同 001 + 检索期缺格式预检 | ✅ 已修复（同 001）→ 重测 PASS |
| ss_robotiq_003 | req_000 | P4_FORMAT* | 首轮 FAIL/P2_RETRIEVE（C4 拦截 0 文件）；检索成功后暴露同类格式问题：xacro 展开器 `_eval_xacro_expr` ast 白名单不含宏参数，`${reflect * -0.0127}` 求值返回空串 → 4 处 `<origin>` 的 xyz/rpy 退化为 2 个数值 → yourdfpy 报 `could not broadcast input array from shape (2,) into shape (3,)` | xacro 表达式求值不支持宏参数参与运算（*P4 同类格式缺陷，检索修复后浮出） | ✅ 已修复（`_eval_xacro_expr(expr, scope)` 求值前整词代入宏参数实参值）→ 重测 PASS |
| ss_google_scanned_002 | req_000 | P4_FORMAT | kettle 词元可命中，但命中 json 元数据（Fuel 目录 JSON）→ MeshSkill 不支持 `file_type=json`（期望 glb/obj/ply），3 轮迭代解析失败，落盘 0 文件 | 检索产物为目录元数据 JSON，未解析出真实 mesh 下载链接 | ✅ 已修复（google_scanned 适配器直接解析 Fuel 模型并下载真实 mesh）→ 重测 PASS_WITH_FALLBACK（期望 OBJ/GLB/PLY、实际落盘 STL，显式 is_fallback） |
| ss_google_scanned_003 | req_000 | P4_FORMAT | 同 002（json 元数据解析失败）；query 为 stopword-only（「大体积」），检索命中质量差 | 同 002 + 查询词构造缺陷 | ✅ 已修复（同 002）→ 重测 PASS_WITH_FALLBACK |
| ms_003 | req_003 | P4_FORMAT | 历史：python 场景（Isaac Lab gen3 场景）落盘 `.xml`（格式错配） | `_ext_for_raw_format` 缺 python→.py 映射 | ✅ 已修复 → 2026-08-17 重测落盘 `sim_config/req_003.py`，12 文件全落盘，PASS_WITH_FALLBACK |

## 2. P5_RUNTIME（运行期/可加载性）

| case_id | req_id | 失败码 | 现象 | 初步定位 | 状态 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| ss_franka_002 | —（URDF mesh 引用） | P5_RUNTIME | 历史：fallback 路径（GitHub 通用源）只落 URDF，11 处 `package://meshes/...` 引用无法解析（网格未随包交付）→ 可加载性失败 | fallback 路径缺「网格资产跟随抓取」 | ✅ 已修复（GitHubAdapter 资产跟随抓取）→ 重测 19 文件（URDF+18 OBJ）LOAD_OK，PASS |
| ss_franka_003 | 同 002 | P5_RUNTIME | 同 002 | 同 002 | ✅ 已修复（同 002）→ 重测 PASS |
| ms_003 | req_003 | P5_RUNTIME（隐患） | 历史：场景引用的资源名与落盘名不一致（assemble 重命名后未回写引用） | assemble 重命名未同步回写 sim_config 内引用 | ✅ 已修复（资源名回写一致性）→ 重测结构核验通过（manifest 12 文件与磁盘一致） |
| ms_003 | req_002 | P5_RUNTIME（残余风险） | GRASP 需求仅落盘 GraspNet json 元数据（completeness 60% / confidence 0.6），非真实 NPZ 抓取姿态数据 | grasp 数据源覆盖不足（GraspNet 大文件无法直接入库） | ⚠️ **残余**：如实记录为显式 fallback（is_fallback=true + fallback_reason），不阻断 PASS_WITH_FALLBACK 判定；建议后续补真实 NPZ 姿态样本 |

## 3. 汇总与统计建议

- **已修复：8 条**（robotiq×3、google_scanned×2、franka×2、ms_003 req_003 + ms_003 资源名隐患），均已在 2026-08-17 前端真实流程重测中验证通过（0 FAIL）。
- **残余：1 条**（ms_003 req_002 GRASP 数据空壳，fallback 如实记录）。
- **建议 F 并入统计的口径**：P4_FORMAT 历史 6 例、P5_RUNTIME 历史 3 例（含 1 隐患）+ 1 例残余；当前 record 集中无新增 P4/P5（validation_issues 均为 0 ERROR）。
- 与 P2_RETRIEVE/P1_PARSE 的边界：robotiq_003 首轮判 P2_RETRIEVE，但检索成功后暴露的是 P4 类格式缺陷，已在表中标注；ms_003 首轮 P2_RETRIEVE（kinova 无适配器）不属本清单（P2 类）。
