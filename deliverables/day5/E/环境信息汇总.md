# Day5 E：环境信息汇总（供 Day6 按模型分桶）

> 角色：E（产品工程师）｜日期：2026-08-18（A 代整理）
> 依据：`records/` 全库 63 条 `record.json` 的 `env` 字段提取（63/63 已判定已复核）
> 说明：本汇总为 Day5 计划「汇总环境信息：各 case 所用 LLM 模型/配置」交付物；数据与 `records/_management/progress.csv` 的 `llm_model` 列一致（脚本 `manage_test_records.py` 同步刷新）。

## 一、模型分桶（63 case）

| 模型 | case 数 | 占比 | 执行人分布 | 备注 |
| ---- | ---- | ---- | ---- | ---- |
| qwen-plus | 61 | 96.8% | C 42 / D 16 / E 2 / F 1 | 全库主力模型，覆盖全部单源题与 6 道多源题 |
| qwen3.7-plus | 2 | 3.2% | A 2 | ms_001 / ms_002（A 08-17 补做多源端到端题） |

## 二、LLM 配置

| 配置项 | 值 | 说明 |
| ---- | ---- | ---- |
| llm_base_url | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 63/63 全部一致（DashScope OpenAI 兼容接口，不含 API Key） |
| mode | `real` | 63/63 全部真实流程（无演示流程记录） |
| review_decision | `satisfied` | 已到达 human_review 的 case 均为 satisfied |

## 三、执行环境（git）

| 分支 | case 数 |
| ---- | ---- |
| feat/arch-langgraph | 63 |

提交分布较分散（C 42 条未记录提交号、D 14 条 `522cd959`、其余含 `9c6b91e`/`3ed8ad8`/`fac6b32` 等），建议 Day6 统计不依赖 git commit 维度。

## 四、按执行人的模型使用

| 执行人 | case 数 | 模型 |
| ---- | ---- | ---- |
| C | 42 | qwen-plus |
| D | 16 | qwen-plus |
| E | 2 | qwen-plus（ms_006 / ms_007） |
| A | 2 | qwen3.7-plus（ms_001 / ms_002） |
| F | 1 | qwen-plus（ms_008，FAIL/P7_ENV） |

## 五、统计建议（供 F 并入 Day6 统计）

- 全库统一走 DashScope qwen 系列，无模型混杂风险；按模型分桶仅两个桶（qwen-plus 61 / qwen3.7-plus 2），差异集中在 A 补做的 2 道多源题，若按模型对比需注意样本量。
- ms_008（qwen-plus）为 P7_ENV 初始化失败，未进入 parse_goal，不参与模型效果统计。
