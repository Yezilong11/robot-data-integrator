# Day6｜C：数据源相关条目验收核对（配合 A 终审）

> 角色：C（数据工程师）｜2026-08-19｜性质：**只读核对**，不修改冻结 `records/`
> 依据：`docs/process/判定口径纪要_C数据源类.md` + 冻结 records/（63 case）+ `records/_management/quality_report.json`（21:36 快照）
> 结论：C 域数据源条目**验收核对通过**；以下 1–2 为合规性核对，3 为口径解读说明（建议 A 拍板口径定义）。

## 1. ss_ieee_001 P7_ENV 豁免 + 兜底可追溯 ✅

- retrieve[0]：source=arxiv、is_fallback=true、fallback_reason=「IEEE 无 API Key（P7_ENV 豁免），经 arxiv 兜底转 complete」。
- 判定 PWF、不阻碍验收线（A 已拍板剔除 ss_ieee_002/003）。✓

## 2. ss_huggingface_001 命中源偏移可追溯 ✅

- retrieve[0]：source=zenodo（**非题设 huggingface**）、is_fallback=true、fallback_reason 与 note 注明「registry 优先级所致」。
- 源偏移显式记录、可追溯，符合"降级必须显式"铁律。✓

## 3. quality 与 is_fallback 口径一致性（建议 A 拍板统一说明）

| case | 命中源 | quality | is_fallback | 解读 |
|---|---|---|---|---|
| ss_graspnet_001 | huggingface（题设 graspnet 兜底）| real | true | 命中源官方直取（real）但走了题设外路径（fallback）——两字段度量维度不同 |
| ms_003 | isaac（最小 MJCF）| real | true | 同上：asset 官方文件（real 存疑，最小 MJCF 实为生成物）|
| ms_006 | isaac（最小 MJCF）| fallback | true | 口径自洽 |

- 核对结论：三例均带 `fallback_reason`/notes，**可追溯 ✓**，不构成记录 ERROR。
- 建议：纪要 §2.1 的 `real` 定义为「从官方源直接获取未走任何兜底」，与"命中源偏移兜底（is_fallback=true）"并存时易误读（day5 遗留问题 4 已提出）。**请 A 裁定**：统一为「quality 仅描述命中源出处，降级一律以 is_fallback 为准」，并在下一轮纪要补充一句定义（P1 级文档修订）。

## 4. 数据源类 P0 抽查（供 A 抽样比对）

| case | P0 | 源 | 判定 | 可用 |
|---|---|---|---|---|
| ss_arxiv_001 | ✓ | arxiv | PWF（显式降级）| ✓ |
| ss_github_001 | ✓ | github | PASS（real）| ✓ |
| ss_huggingface_001 | ✓ | zenodo 兜底 | PWF（源偏移显式）| ✓ |
| ss_huggingface_002 | ✓ | 混合 | PWF（显式降级）| ✓ |
| ss_graspnet_002 | ✓ | huggingface 兜底 | PWF（R1 显式降级）| ✓ |

> 与 quality_report 一致：P0 可用 11/11（含 D 域），≥ 目标 8/11 ✓。

## 5. 核对结论

C 域数据源条目**验收核对通过**：记录完整率 100%、截图合规、无静默降级、统计与记录一致；遗留 3 项（源偏移、相关性、quality 定义）已逐条给出核对意见，交 A 终审拍板。
