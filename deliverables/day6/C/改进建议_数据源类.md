# Day6｜C：数据源类改进建议清单（提交 F 并入《改进建议清单》）

> 角色：C（数据工程师）｜2026-08-19
> 范围：数据源类改进建议（分 P1/P2，指向模块；数据源类本轮无 P0 必改项，ms_008 教训属目标解析域、不在数据源类范围）；由 A 复盘会定性定级后并入 F 的《改进建议清单》终稿
> 依据：`P2P3归因清单.md` + `docs/problem_set/问题集执行_已知风险.md`（R1/R2）+ `data_source_availability.md` §五

## P1（应改，消除残余风险）

| # | 建议 | 指向模块 | 依据 / 验证方式 |
|---|---|---|---|
| P1-1 | **retriever 候选源优先级/注入逻辑核对**：确认 paperswithcode / dexgrasp（探活 OK 但 0 直接命中）是否被候选源排序绕过，或为执行期随机性；核对候选源列表与注入顺序 | `src/rdi/adapters/registry.py` + `src/rdi/graph/nodes/retrieve_data.py` | P2P3归因清单 §4。验证：同输入重复执行，命中源分布稳定且优先命中题设源 |
| P1-2 | **zenodo 检索相关性**：search 关键词策略改进，避免命中无关主题（deepfakes/巴西选举）| `src/rdi/adapters/zenodo.py` | ss_huggingface_001。验证：robot grasp 类场景不再命中无关主题记录 |
| P1-3 | **per_req_timeout 预算自适应**：按源/按需求类型配置超时预算，慢源（mujoco 68 资产 ~42s）不再超 60s 默认预算 | `src/rdi/config/settings.py` + 检索预算分配 | ss_mujoco_001（60s→180s 才 PASS）。验证：默认预算下 mujoco 类需求不再 per_req 超时 |
| P1-4 | **quality 口径定义补充**：明确「quality 描述命中源出处；降级一律以 is_fallback 为准」，消除 quality=real + is_fallback=true 的误读 | `docs/problem_set/判定口径纪要_C数据源类.md` §2.1（文档修订）| 验收核对 §3。验证：纪要更新后 ss_graspnet_001 / ms_003 判定口径无歧义 |

## P2（可改，提升质量档）

| # | 建议 | 指向模块 | 依据 / 验证方式 |
|---|---|---|---|
| P2-1 | huggingface real 命中率提升：单仓库大文件直链/分片下载，突破「0 real」| `src/rdi/adapters/huggingface.py` | 12 命中全 is_fallback。验证：12 命中中 real > 0 |
| P2-2 | graspnet 本地数据目录挂载：`local_datasets.graspnet` 指向已解压子集，出真实 npz | `src/rdi/config/settings.py`（R1 升级路径①）| ss_graspnet_002。验证：GRASP 需求产出真实 npz 而非元数据 |
| P2-3 | dexgrasp 物体覆盖扩充：扩充 `_DEXGRASP_DATASET_FILES` 覆盖 apple/scissors（需官方文件存在）| `src/rdi/adapters/dexgrasp.py`（R2 升级路径）| ss_dexgrasp_002/005。验证：覆盖外物体可出真实 npy |
| P2-4 | github 无 token 限流风险：文档/配置提示 + 限流重试策略 | `src/rdi/adapters/github.py` + 文档 | 可用性表遗留问题。验证：连续执行无 403/429 中断 |

## 认领（C）

- C 认领以上**全部数据源类条目**（P1-1~4、P2-1~4），复盘会后由 A 定级后进入下一轮实现。
- 提交 F：并入《改进建议清单》初稿（分 P0/P1/P2，指向模块）。

## 下一轮问题集建议（数据源类边界场景升级，提交 A 汇总）

| # | 建议题目 | 升级自 | 验收要点 |
|---|---|---|---|
| N2 | graspnet 真实 grasp 单文件题：要求真实 npz 落盘 | R1 / ss_graspnet_002 | P2-2 落地后：产出真实 npz 而非元数据 |
| N3 | dexgrasp 覆盖外物体题：apple/scissors 换源可达 | R2 / ss_dexgrasp_002/005 | P2-3 落地后：覆盖外物体可出真实数据 |
| N4 | huggingface real 直链题：要求 real 命中 | P2-1 | P2-1 落地后：命中质量=real 且 is_fallback=false |
