# Day6｜C：数据源类 P2_RETRIEVE / P3_SOURCE 归因清单

> 角色：C（数据工程师）　整理日期：2026-08-19
> 依据：`docs/process/Day6_汇总统计.md`（C 任务 2）+ `docs/process/判定口径纪要_C数据源类.md` §5 分类码映射 + 冻结 `records/`（63 case，2026-08-18 A 复核）
> 范围：**数据源类** P2_RETRIEVE / P3_SOURCE 归因（P4_FORMAT/P5_RUNTIME 属 D，P6_FRONTEND 属 E，P7_ENV 仅 ieee 豁免项由 C 记录源侧事实）
> 说明：每条含 case_id、分类码、现象、根因（**指向具体 adapter / node / settings 或上游限制**）、证据、状态、**根因定性候选**（供 A 复盘定性：系统缺陷 / 问题集设计不当 / 环境因素 / 数据源外部限制）

## 1. 边界说明：ms_008 不属 C 数据源类归因范围

全库唯一 FAIL（ms_008，分类码 `P2_RETRIEVE`，A 按 `docs/process/问题集执行_已知风险.md` R4 拍板记录）**根因在 `parse_goal.py` 的 fallback_sources 选源随机性（目标解析域），非数据源可用性问题**，不在 C 数据源类归因范围，C 不归因；其根因与改进由目标解析域负责（见 `docs/process/问题集执行_已知风险.md` R4 升级路径）。

## 2. 历史 P2_RETRIEVE → 已解决（当前 PASS/PWF，记录 notes 可追溯）

| case_id | 分类码 | 现象 | 根因指向 | 证据 | 状态 | 定性候选 |
|---|---|---|---|---|---|---|
| ms_005 | P2→PWF | 旧记录 FAIL：Franka Panda URDF 检索 3 轮失败——franka 源超时 + 通用 GitHub 返回 markdown 类型错配被 C4 拦截，2 条 robot_urdf 需求缺失，落盘仅 1 文件 | ① franka 源 fetch 超时（per_req 预算内）；② `src/rdi/adapters/github.py` 通用源对 robot_urdf 返回 markdown，检索期缺格式预检 | ms_005/record.json notes | ✅ 已解决：重测 panda.urdf 直连 pybullet_robots（CDN jsdelivr，13/12）+ YCB mesh fuel 兜底 + scene.xml 直连，90 文件，PWF | 系统缺陷（已修复）|
| ms_003 | P2→PWF | 旧记录 FAIL：Kinova 无适配器、主源检索失败 | ADAPTER_REGISTRY 未注册 Kinova → 新增 `src/rdi/adapters/kinova.py` 命中 ros_kortex | ms_003/record.json notes | ✅ 已解决：重测 4 需求全 fulfilled、12 文件全落盘、missing=0，PWF（req_000/002/003 显式 fallback）| 系统缺陷（已修复）|
| ss_huggingface_001 | P2→PWF | 旧记录 FAIL：github 首源检索超时；修复后源级子预算生效，最终命中源为 **zenodo**（非题设 huggingface），且返回数据集主题为 deepfakes/巴西选举（非 robot grasp），相关性存疑 | ① github 首源超时（源级子预算已修复）；② **命中源偏移**：retriever 候选源优先级/注入逻辑（registry 优先级）使 zenodo 兜底命中；③ zenodo search 相关性弱 | ss_huggingface_001/record.json（retrieve[0] source=zenodo + notes）| ⚠️ **已解决但残余**：PWF 已复核；命中源偏移 + 相关性存疑 → P1 改进（retriever 优先级核对 / zenodo 检索相关性）| 系统缺陷（源选择/相关性，部分修复）+ 数据源外部限制（zenodo 相关性）|
| ss_mujoco_001 | P2→PASS | 首次 PER_REQ_TIMEOUT=60s：mujoco fetch 需串行下载 68 个 mesh 资产（~42s）+ franka 并行检索双需求超预算 → per_req_timeout 超时 FAIL（网络/超时预算类，非格式问题）；重跑 180s 成功 | `src/rdi/config/settings.py` per_req_timeout=60s 对 mujoco 大场景过紧（慢源现实 vs 固定预算）| ss_mujoco_001/record.json notes | ✅ 已解决：180s 重跑 PASS（88 文件，URDF/Mesh/MJCF 可加载三类全过）| 环境/配置因素（超时预算）+ 数据源外部限制（mujoco 资产多、下载慢）|
| ss_allegro_001~003 | P2→PASS | 旧记录 FAIL：通用 GitHubAdapter 返回 markdown 类型错配、落盘 0 文件 | `src/rdi/adapters/github.py` 通用源未命中专用仓库；修复后 `src/rdi/adapters/allegro.py` 专用源命中 dexsuite/dex-urdf 仓库 allegro_hand_right.urdf | ss_allegro_001~003/record.json notes | ✅ 已解决：3 条 PASS（23 links/22 joints、11/11 mesh 可加载）| 系统缺陷（已修复）|

## 3. P3_SOURCE 上游限制组（残余，如实记录，不判执行人责任）

| 源 | 现象 | 根因（上游限制）| 影响 case | 状态 |
|---|---|---|---|---|
| graspnet | grasp 标注仅存在于大 tar 归档（grasp_label.tar ~25.9GB、models.tar ~12.8GB），无单文件 `.npz/.pkl` | **数据源形态限制**（HF 镜像仓库只托管 tar），非代码 bug（R1）| ss_graspnet_002（P0）等 GRASP 需求 | 残余：fetch 返回 metadata JSON + RawReference 显式降级 → PWF |
| dexgrasp | 官方 raw 单物体 `.npy` 兜底仅覆盖 6 物体（banana/mug/bottle/camera/plant/plate）；apple/scissors 官方仓库无独立单文件 | **数据源覆盖限制**（R2）| ss_dexgrasp_002（apple）、ss_dexgrasp_005（scissors）| 残余：未覆盖物体走 metadata JSON 降级 → PWF |
| isaac | 无真实 Isaac Sim 资产，仅最小 MJCF fallback 可达 | **数据源资产不可得**（无真实场景文件）| ms_003、ms_006 | 残余：显式降级 → PWF |
| huggingface | 12 条命中全部 `is_fallback=true`，0 real | **数据源形态 + 适配器降级路径**：model_info 404 → config → metadata；单仓库大文件无直链 | 12 条（含 5 条 dexgrasp 兜底、2 条 ycb grasp 兜底）| 残余：显式降级 → PWF |
| ieee | 无 API Key，检索/下载跳过 | **环境因素**（blocked-by-user，P7_ENV 豁免，A 拍板）| ss_ieee_001 | 残余：经 arxiv 兜底 → PWF，不阻碍验收线 |

## 4. 待核实项

| 源 | 现象 | 待核实内容 | 指向 |
|---|---|---|---|
| paperswithcode / dexgrasp | 探活 search/fetch OK，但 records/ 中无直接命中记录（实际经 arxiv / huggingface 兜底）| retriever 候选源优先级与注入逻辑是否导致执行期直接绕过两源，或为候选源排序/执行期随机性 | `src/rdi/adapters/registry.py` 候选源列表与 `retrieve_data.py` 注入顺序（列入 P1 改进建议，见 `改进建议_数据源类.md` P1-1）|

## 5. 汇总结论（提交 F/A）

- **数据源类冻结记录无 FAIL**；全库唯一 FAIL（ms_008）根因在 parse_goal 选源随机性（目标解析域，非数据源类，见 §1 边界说明）。
- **历史 P2 已修复 5 组**（ms_005 / ms_003 / ss_huggingface_001 / ss_mujoco_001 / ss_allegro_001~003），当前均为 PASS/PWF，记录 notes 可追溯。
- **P3_SOURCE 上游限制残余 5 项**（graspnet / dexgrasp / isaac / huggingface / ieee），均按"显式降级可追溯"口径记 PWF，不判执行人责任（纪要 §2.4 / R1 / R2）。
- **待核实 1 项**（paperswithcode / dexgrasp 源优先级）。
- 与 P4/P5 边界：robotiq_003 首轮判 P2_RETRIEVE 但实为 P4 类格式缺陷（D 清单已标注）；ms_003 首轮 P2_RETRIEVE（kinova 无适配器）不属 D 清单，归 C 本清单第 2 节。
