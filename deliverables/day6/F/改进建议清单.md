# Day 6｜改进建议清单（F 汇总初稿）

> 依据：63 条冻结记录、`records/_management/quality_report.json` 严格快照、C 的数据源归因、D 的格式/运行时结论、E 的环境汇总。
>
> 状态：F 汇总初稿。优先级需由 A 在 Day6 复盘终审时确认；P0 表示进入下一轮正式执行前必须处理，P1 表示应在下一轮测试前关闭或建立可追溯豁免，P2 表示可随问题集迭代处理。

## P0：阻断下一轮稳定性的事项

| ID | 问题与证据 | 目标模块 | 建议动作 | 验收方式 | 建议责任 |
|---|---|---|---|---|---|
| P0-01 | `ms_008` 中 `parse_goal` 将 Franka URDF 候选源随机选为 `allegro/robotiq/kinova`，三轮全 miss，最终 `FAIL/P2_RETRIEVE`；同输入探针可经 GitHub 成功。 | `src/rdi/graph/nodes/parse_goal.py` 的 `_normalize_datareq`，以及目标解析 prompt | 对厂商 `robot_urdf` 建立确定性映射：Franka 至少优先 `franka -> github`；Kinova、Robotiq、Allegro 同理。把题设 source 作为硬约束或高优先级候选，而非让 LLM 自由生成。 | 新增单测：即使 LLM 返回错误候选源，`Franka Panda URDF` 最终候选源仍为 `franka/github`；增加 `ms_008` 真实流程或可控端到端回归，输出可用 URDF 包。 | A / 目标解析负责人 |
| P0-02 | 当前对同输入的前端和探针结果不同，无法判断是 LLM 采样、候选源顺序还是前端状态差异。 | `parse_goal`、`retrieve_data`、前端运行日志 | 记录解析后 `DataReq`、候选源列表、每源搜索/下载结果和随机/模型参数，作为 case 证据的一部分；前端和脚本使用同一调用路径。 | 同输入连续运行至少 3 次，可直接对比候选源顺序与结果；若仍不一致，日志能定位差异阶段。 | E / A |

## P1：质量与可复现性事项

| ID | 问题与证据 | 目标模块 | 建议动作 | 验收方式 | 建议责任 |
|---|---|---|---|---|---|
| P1-01 | C 观察到 PapersWithCode、DexGrasp、IEEE 的实际命中常偏移到兜底源；`ss_huggingface_001` 的 Zenodo 命中相关性存疑。 | `src/rdi/adapters/registry.py`、`src/rdi/graph/nodes/retrieve_data.py` | 明确“题设源 -> 首选 -> 可接受兜底”的优先级，结果中记录 `requested_source`、`selected_source`、`fallback_reason`，对 Zenodo 增加主题/关键词相关性阈值。 | 针对源偏移样例写回归测试：目标源可用时不得先被无关兜底抢占；不相关结果不得判成功。 | C |
| P1-02 | GraspNet 大 tar 无单文件、DexGrasp 对 apple/scissors 覆盖不足、HuggingFace 多为 metadata、Isaac 无真实资产，造成大量合规 PWF。 | GraspNet / DexGrasp / HuggingFace / Isaac Adapter，`local_datasets` 配置 | 提供可选本地解压数据目录和小样本；为 HuggingFace 支持真实文件直链；记录每个对象的覆盖范围；没有真实资产时保持显式 fallback。 | 新增真实 NPZ/PKL、未覆盖物体、离线本地数据三组回归；输出中准确标注 real/fallback。 | C / D |
| P1-03 | MuJoCo 大场景曾受固定源级预算影响；当前 `per_req_timeout` 仍按候选源数均分。 | `src/rdi/config/settings.py`、`retrieve_data.py` | 按需求类型、资产数量或 Adapter 特征配置预算；保留总预算并避免一个慢源耗尽所有候选源机会。 | 大型 MuJoCo 场景、Kinova URDF 资产跟随下载均在预算内完成；超时日志能说明已尝试的源。 | C / D |
| P1-04 | `quality=real` 与 `is_fallback=true` 在少量源偏移记录并存，统计含义容易误读。 | `src/rdi/skills/registry.py`、manifest schema、记录模板 | 明确字段责任：来源真实性与是否相对题设/主源降级分开定义；校验器直接检查矛盾组合并要求 reason。 | 给真实主源、镜像、跨源兜底、metadata 四类 fixture；统计能稳定区分真实性和降级。 | F / C |
| P1-05 | 28 条 WARNING 来自 `package.dir` / `manifest_path` 的跨机器绝对路径；当前仓库无法直接加载这些本地包。 | assemble/manifest、records schema、归档流程 | 使用项目相对路径、内容哈希或归档附件索引；记录每个包的可复核位置和文件校验值。 | 在另一台机器 clone 后，至少能定位/下载抽样包并复核 manifest 与截图。 | F / D |
| P1-06 | IEEE 无 API Key 时只能被标为 blocked 后经 arXiv 兜底，环境原因与系统失败容易混淆。 | IEEE Adapter、环境预检、record schema | 开测前验证 Key 可用性；不可用时将 case 标记为预先批准的环境豁免，并在报告中单列，不混入 Adapter 成功率。 | 无 Key / 有 Key 两种环境都能生成一致、可追溯的记录。 | C / E |
| P1-07 | D 的历史 P4/P5 已修复，但 `ms_003` 仍只有 grasp metadata，格式/运行时证据只能覆盖部分真实资产。 | Grasp Skill、加载性验证脚本 | 对 PWF 包保留“降级前后资产类型”字段；将真实 grasp 姿态加载加入 D 的自动检查。 | `ms_003` 或替代场景有真实 grasp 文件时，完成解析与加载；无文件时保持 PWF 而不虚报 PASS。 | D |

## P2：文档、审计与下一轮题库事项

| ID | 问题与证据 | 目标模块 | 建议动作 | 验收方式 | 建议责任 |
|---|---|---|---|---|---|
| P2-01 | `assignments.csv` 计划 F 复核 62 条，但实际 A 复核了全部 63 条；校验器只验证 reviewer 合法。 | 分配表、记录 schema、`manage_test_records.py` | 增加 `reviewer_override_reason` 或将 A 终审覆盖写入分配表；校验计划复核人与实际复核人的授权关系。 | 发生替代复核时，报告不再出现无法解释的计划/实际差异。 | F / A |
| P2-02 | 旧文档混有“Day8 严格验收”、65 题/57 单源、`ms_008=P7_ENV` 等历史快照。 | README、冻结报告、历史交付索引 | 保留历史文件但加“已被何时何物取代”说明；运行说明改称“Day6-8 合并收尾严格验收”。 | 搜索 `P7_ENV` / `65 题` / `Day8` 时，所有非历史位置都有明确的版本或替代说明。 | F |
| P2-03 | 代码注册的 `kinova` 未被正式题设 source 覆盖，题库校验持续给出 1 条 WARNING。 | `problem_set/problem_set.json`、题库评审清单 | 新增 Kinova 单源题，或把 Kinova 明确标为只在多源/回归中覆盖并调整校验规则。 | `validate-problems` 无未解释 WARNING，或 WARNING 有已签收的豁免。 | A / C / D |
| P2-04 | 现有题库的单源/多源、语言和边界扰动覆盖不足以量化 LLM 非确定性。 | 下一轮 `problem_set` | 新增同目标多次运行、题设源冲突、无目标关键词、真实数据/metadata 对照、大型资产超时、无 Key 等边界 case。 | 每个关键风险至少有 1 个稳定回归 case，失败能定位至 P1-P8 的单一主因。 | F / 全员 |

## 复盘决议记录

| 决议项 | A 裁定 | 日期 | 备注 |
|---|---|---|---|
| P0-01 是否按 P0 进入下一轮阻断项 | 待裁定 | — | F 建议按 P0 处理，因为它已导致真实前端 FAIL。 |
| P0-02 日志与重复运行基线 | 待裁定 | — | 建议与 P0-01 同批进入回归。 |
| P1/P2 责任人与完成期限 | 待裁定 | — | 交付后由 A 在复盘会补全。 |

