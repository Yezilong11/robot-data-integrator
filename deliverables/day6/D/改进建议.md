# Day 6｜D：格式/仿真类改进建议（初稿，交 F 合并）

> 角色：D（机器人工程师）　初稿日期：2026-08-17
> 依据：`docs/problem_set/Day6_汇总统计.md` D 任务 4 + `P4_P5归因.md`（10 项归因）
> 说明：优先级（P0 必改 / P1 应改 / P2 可改）为 D 初判，复盘会上按 A 裁定调整；每条含指向模块、预期收益、工作量估计。

## P0（必改）

| # | 建议 | 指向模块 | 归因对应 | 预期收益 | 工作量 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| A1 | **Mesh 格式协商机制**：MeshSkill/适配器按题设期望格式（glb/obj/ply…）协商输出，无法满足时再显式 is_fallback，而非默认 STL | skill: MeshSkill + adapter: google_scanned | 归因 #1/#2/#7 | google_scanned×3、ms_003 req_001、ms_005 等格式类 PWF 收敛为 PASS；消除「真实命中被判格式降级」 | 中 |
| A2 | **GraspNet 真实 NPZ 抓取数据支持**（R1）：grasp adapter 支持单文件 .npz/.pkl 检索与落盘，或补齐结构化元数据的完整抓取姿态字段；需 C 协同确认源 | adapter/skill: grasp | 归因 #8/#9（跨 6+ case 系统级） | 消除最大规模的 PWF 群（grasp 空壳），ms_001/002/003/006/007、ycb_004/005 受益 | 重 |

## P1（应改）

| # | 建议 | 指向模块 | 归因对应 | 预期收益 | 工作量 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| B1 | **格式转换显式化**：obj→stl、trimesh 兜底等转换动作写入 transformations 记录并保留原始格式副本；「命中真实源 + 格式转换」与「降级」判定解耦 | node: assemble | 归因 #2/#7 | 判定口径更清晰，减少 A 合规化人工修正（如 gs 系列 PASS→PWF 重判） | 轻 |
| B2 | **sim_config skill：Isaac Python 场景模板 + 静态校验**：.py 场景生成模板化（资源引用回写、随包网格完整性、格式自检），并提供 sim_config 静态校验（存在性/引用一致性） | skill/node: sim_config | 归因 #4 | ms_003 req_003 类语义错配不再依赖人工核验；仿真配置交付质量可测 | 中 |

## P2（可改）

| # | 建议 | 指向模块 | 归因对应 | 预期收益 | 工作量 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| C1 | **xacro 展开器健壮性**：已修复宏参数代入（robotiq），补外部宏 include 链/通用表达式覆盖，扩充样例测试（robotiq/allegro 等） | adapter: robotiq | day5 历史 P4_FORMAT | 降低 URDF 类源格式解析回归风险 | 轻 |
| C2 | **可加载性验证脚本入库**：`data/_verify_loadability.py`（yourdfpy/trimesh/MuJoCo 三件套）固化为工具链，纳入 CI/预检 | 工具链 | day5 验证链路 | 每次打包后自动复验可加载性，形成质量红线 | 轻 |
| C3 | **YCB 官方源下载路径改进**：官方模型页提供可直接下载的单 mesh 资源或规范化解析器（与 C 协同）；quality=unknown 时补充标注策略 | adapter: ycb | 归因 #7/#10 | ycb 系列（ycb_002~005）PWF 收敛 | 中 |

## 汇总

- P0×2（A1 格式协商、A2 GraspNet NPZ）——对应最大 PWF 群：格式类 3+ 项 + grasp 类 6+ 项。
- P1×2（B1 转换显式化、B2 sim_config 模板）——提升判定口径与仿真配置交付质量。
- P2×3（C1 xacro 健壮性、C2 验证工具入库、C3 ycb 源路径）——风险兜底与工具链固化。
- 认领：A1/B1/B2/C1/C2 为 D 认领项；A2/C3 与 C 协同（grasp/ycb 数据源侧）；D 在复盘会按 A 裁定确认最终归属。
